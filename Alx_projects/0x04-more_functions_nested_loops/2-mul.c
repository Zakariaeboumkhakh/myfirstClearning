/*Write a function that multiplies two integers.
 *
 * Prototype: int mul(int a, int b);
 * author: ECHARRAD AHMED
 */
#include <stdio.h>
int mul(int a, int b);

int mul(int a, int b)
{
	int mul = a * b;
	return (mul);
}
