/**
 * Write a program that prints the address of env (the third parameter of the main function) and environ (the global variable). Are they th
 * y same? Does this make sense?
 */
#include <stdio.h>
#include <stdlib.h>
int main(int ac , char *av[] , char *env[])
{

